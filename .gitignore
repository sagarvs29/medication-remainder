########################################
# Keep only the medication app project #
########################################

# Ignore everything by default
*

# Keep this .gitignore itself
!.gitignore

# Keep only the project folder and everything inside it
!medication-remainder/
!medication-remainder/**

# Optionally keep VS Code settings inside the project folder if present
!.vscode/
!.vscode/**